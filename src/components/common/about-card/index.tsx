import styles from "./AboutCard.module.scss";

type Props = {
  src: string;
  title: string;
  description: string;
};

const SectionTitle = ({ src, title, description }: Props) => {
  return (
    <>
      <div>
        <p></p>
      </div>
    </>
  );
};

export default SectionTitle;
