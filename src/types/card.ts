export type AboutCardProps = {
  key: string;
  src: string;
  title: string;
  description: string;
};

export type SkillCardProps = {
  index: string;
  src: string;
  skill: string;
  description: string;
  level: number;
};
